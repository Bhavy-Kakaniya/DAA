#include <stdio.h>

int bubble_sort(int length, int arr[])
{
    for(int i = 0; i < length - 1; i++) {
        for(int j = 0; j < length - 1; j++){
            
        }
    }
}

int main()
{
    int arr[] = {4, 5, 7, 6, 10, 9, 8, 1, 2, 3};
    int length = sizeof(arr) / sizeof(arr[0]);
}